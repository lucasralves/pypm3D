def title(title: str, verbose: bool) -> None:
    if verbose: print(' - {}'.format(title))
    return

def subtitle(title: str, verbose: bool) -> None:
    if verbose: print('   > {}'.format(title))
    return